" To resolve links
  let g:rooter_resolve_links = 1
" To change directory for the current window only
  let g:rooter_use_lcd = 1
" Change to home directory on non-project files
  let g:rooter_change_directory_for_non_project_files = 'home'
